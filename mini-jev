#!/usr/bin/env bash
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
if [ -f "$SCRIPT_DIR/.venv/bin/mini-jev" ]; then
    exec "$SCRIPT_DIR/.venv/bin/mini-jev" "$@"
else
    exec python3 "$SCRIPT_DIR/cli.py" "$@"
fi
